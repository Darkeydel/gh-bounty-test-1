name: bounty-probe
on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Dump event context
        run: echo "${{ toJson(github) }}" | head -c 2000
      - name: Print payload ref
        run: echo "payload ref"
